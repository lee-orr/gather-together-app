spin_version = "1"
authors = ["lee-orr <lee-orr@users.noreply.github.com>"]
description = ""
name = "api-endpoints"
trigger = { type = "http", base = "/" }
version = "0.1.0"

[[component]]
id = "api-endpoints"
source = "target/wasm32-wasi/release/api_endpoints.wasm"
allowed_http_hosts = []
[component.trigger]
route = "/hello/..."
[component.build]
command = "cargo build --target wasm32-wasi --release"

[[component]]
id = "api-endpoints"
source = "target/wasm32-wasi/release/api_endpoints.wasm"
allowed_http_hosts = []
[component.trigger]
route = "/goodbye/..."
[component.build]
command = "cargo build --bin goodbye --target wasm32-wasi --release"
