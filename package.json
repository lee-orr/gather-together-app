{
    "scripts": {
        "spin": "cd sign-management-api && cargo watch --poll -s \"spin build --up\"",
        "admin": "cd admin-panel && npm install && npm run start",
        "volunteer": "cd volunteer-app && npm install && npm run start"
    },
    "devDependencies": {
        "concurrently": "*"
    }
}