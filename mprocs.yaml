
procs:
  admin:
    shell: "npm run admin"
  volunteer:
    shell: "npm run volunteer"
  api-bacon:
    shell: "bacon"
    cwd: "./api"
  api:
    shell: "cargo watch -s \"spin build && spin up\""
    cwd: "./api"